## [Manim](https://www.manim.community/) (Math Animation) Playground

### To build locally

```bash
npm install
pip install -r requirements.txt
npm run build
make html
```

Entry point is `build/html/index.html`